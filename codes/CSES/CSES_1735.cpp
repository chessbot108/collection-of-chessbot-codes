#include <iostream>
#include <cassert>
#include <algorithm>
#include <cstring>
#define LC(n) (((n) << 1) + 1)
#define RC(n) (((n) << 1) + 2)
#define ll long long
using namespace std;
const int LOGN = 20, max_v = (1 << LOGN)*4;

 
int main(){
    cin.tie(0) -> sync_with_stdio(0);
    
    return 0;
}